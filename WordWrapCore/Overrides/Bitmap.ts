import { emptyString } from '../Shared/_Strings';
import { CoreWrapParams } from '../Structures/CoreWrapParams';

// May need to remove this file at some point

var oldMeasureTextWidth = Bitmap.prototype.measureTextWidth;
